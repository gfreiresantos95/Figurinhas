<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash theme. -->
    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_scarlet</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Figurinhas</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Figurinhas" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_scarlet</item>
        <item name="colorPrimaryVariant">@color/dark_scarlet</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">?attr/colorOnPrimary</item>
    </style>

    <style name="Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">?attr/colorOnPrimary</item>
        <item name="titleTextColor">?attr/colorPrimary</item>
    </style>
</resources>